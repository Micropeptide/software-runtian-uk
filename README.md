# software.runtian.uk

Auto-generated "Software" showcase page for [runtian.uk](https://runtian.uk), mirroring
Robert's public GitHub repos (github.com/Micropeptide).

**Do not hand-edit `index.html` or `assets/` — they're regenerated.** The only files meant
to be edited directly are `ci/sync.py` (the generator) and `.github/workflows/sync.yml`
(the schedule that runs it every 6 hours via GitHub Actions, also triggerable manually
from the Actions tab).

A development copy of this same pipeline (used for local testing before changes are
ported into `ci/sync.py`) lives at
`/Users/runtianwu/Rdirectory/WebDevelop/sites/runtian-uk/software-mirror/` on Robert's Mac.
